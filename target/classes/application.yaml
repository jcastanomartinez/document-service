spring:
  datasource:
    url: jdbc:postgresql://172.17.0.2:5432/documentdb
    username: document_user
    password: document_pass
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

server:
  port: 8080
